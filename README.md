## Esoft
To run this project check for the ```.env```(you can copy all lines from ```.env.example``` and paste them in ```.env```) file and:

```
make up
```

And we have swagger in localhost:8080/docs/index.html
